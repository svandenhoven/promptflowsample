[
    {
        "name": "createTask",
        "description": "Create a new task with title and description",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "The title of the task to create"
                },
                "description": {
                    "type": "string",
                    "description": "The detailed description of the task to create"
                }
            },
            "required": [
                "title",
                "description"
            ]
        }
    },
    {
        "name": "deleteTask",
        "description": "Delete an existing task",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {
                    "type": "string",
                    "description": "The title of the task to delete"
                }
            },
            "required": [
                "title"
            ]
        }
    },
    {
        "name": "answerWikipediaQuestion",
        "description": "Answer a question using Wikipedia",
        "parameters": {
            "type": "object",
            "properties": {
                "question": {
                    "type": "string",
                    "description": "The question to get the answer from Wikipedia"
                }
            },
            "required": [
                "question"
            ]
        }
    }
]